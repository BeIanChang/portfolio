r# Architecture

- Framework: Next.js (static export)
- Rendering: static HTML + minimal client JS
- Hosting: Netlify
- Backend: none (external links only)

The site should be deployable as a fully static artifact.

Future backend services (if any) live outside this repository.
